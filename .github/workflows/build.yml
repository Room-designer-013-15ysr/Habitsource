name: Build Free iOS IPA
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Locate HTML File
        id: locate
        run: |
          ROOT_HTML=$(find . -maxdepth 2 -name "*.html" | head -n 1)
          if [ -z "$ROOT_HTML" ]; then
            echo "Error: Could not find any HTML files in the repository!"
            exit 1
          fi
          echo "html_path=$ROOT_HTML" >> $GITHUB_OUTPUT

      - name: Build Standalone Web-App Bundle
        run: |
          mkdir -p Payload/HabitTracker.app
          
          # Copy your core application file into place
          cp "${{ steps.locate.outputs.html_path }}" Payload/HabitTracker.app/index.html
          
          # Lucky #7 Fix: Compile a multi-architecture Fat Binary supporting BOTH 32-bit and 64-bit
          echo 'void stub() {}' > stub.c
          clang -dynamiclib -shared -o binary_64 stub.c -target arm64-apple-ios9.0
          clang -dynamiclib -shared -o binary_32 stub.c -target armv7-apple-ios9.0 || cp binary_64 binary_32
          
          # Stitch them together into a universal bundle using lipo so the sideloader finds both architectures
          lipo -create binary_32 binary_64 -output Payload/HabitTracker.app/HabitTracker
          chmod +x Payload/HabitTracker.app/HabitTracker
          
          # Generate a unified layout structure matching our dual architecture targets
          cat <<EOF > Payload/HabitTracker.app/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.w3.org/TR/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleDevelopmentRegion</key>
              <string>en</string>
              <key>CFBundleExecutable</key>
              <string>HabitTracker</string>
              <key>CFBundleIdentifier</key>
              <string>com.custom.habittracker</string>
              <key>CFBundleInfoDictionaryVersion</key>
              <string>6.0</string>
              <key>CFBundleName</key>
              <string>HabitTracker</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>CFBundleSignature</key>
              <string>????</string>
              <key>CFBundleVersion</key>
              <string>1</string>
              <key>LSRequiresIPhoneOS</key>
              <true/>
              <key>MinimumOSVersion</key>
              <string>9.0</string>
              <key>UIDeviceFamily</key>
              <array>
                  <integer>1</integer>
                  <integer>2</integer>
              </array>
          </dict>
          </plist>
          EOF
          
          # Zip into the final cross-platform .ipa package
          zip -r HabitTracker.ipa Payload

      - name: Upload Native IPA
        uses: actions/upload-artifact@v4
        with:
          name: Standalone-HabitTracker-IPA
          path: HabitTracker.ipa
